# 财务数据库查询结果处理与前端展示 - 技术文档

## 重要说明
1. 本文档是copy自D:\MyProjects\MCP_coze项目，不可照搬到fintax_ai项目，而是要根据fintax_ai项目实际情况适当参考借鉴即可
2. 不文档中的脚本、项目结构以及数据库名称和结构都copy自供参考项目
3. 本文档中的方案/方法可能不是最优，需要仔细分析甄别，如果有更优、更适合fintax_ai项目的，则须以最优方案/方法替代
4. 本文档中与fintax_ai原9域查询逻辑冲突，则须给出详细提示供确认

---

## 一、系统架构概述

财务数据查询系统采用前后端分离架构，通过 SSE（Server-Sent Events）实现实时流式数据传输，支持文本内容与图表数据的混合推送。

### 1.1 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 后端 | FastAPI + Python | RESTful API 服务 |
| 数据库 | SQLite | financial.db 企业财务数据库 |
| 前端 | React + Chart.js | UI 渲染与图表展示 |
| 实时通信 | SSE (Server-Sent Events) | 服务端推送 |

### 1.2 数据流架构

```
用户问题
    │
    ▼
┌─────────────────────────────────────────────────┐
│  FastAPI /chat/stream 端点                      │
│  (server/routers/chat.py)                      │
└─────────────────────────────────────────────────┘
    │
    ├──▶ FinancialQuery.search() ──▶ financial.db
    │       │
    │       ├── 格式化方法: format_results()
    │       │   └── 返回 Markdown 表格/列表
    │       │
    │       └── 图表数据生成: 内存中构建 chartData
    │           └── yield send_event("chart", chartData)
    │
    ├──▶ SSE Event: "message" ──▶ 前端渲染文本
    │
    └──▶ SSE Event: "chart" ──▶ 前端 ChartRenderer
            │
            └── 图表组件渲染
```

---

## 二、后端查询模块详解

### 供参考项目D:\MyProjects\MCP_coze\modules/financial_query.py`方案(略)
### fintax_ai项目现有9个域查询逻辑不变
---

## 三、结果格式化详解


### 3.1 格式化策略选择

供参考方案文件: `modules/financial_query.py`

```python
def format_results(self, results: List[Dict], company: Dict) -> str:
    """
    格式化查询结果
    当指标 > 1 或期间 >= 4 时使用表格格式
    """
    # 统计不同指标和期间
    metrics = set(r['metric_name'] for r in results)
    periods = set((r['year'], r.get('quarter')) for r in results)
    
    # 判断是否使用表格格式
    use_table = len(metrics) > 1 or len(periods) >= 4
    
    if use_table:
        return self._format_as_table(results, metrics, periods)
    else:
        return self._format_as_list(results)
```

### 3.2 表格格式输出

供参考方案文件: `modules/financial_query.py`，行号 1255-1292

```python
def _format_as_table(self, results, metrics, periods) -> str:
    """生成 Markdown 表格格式"""
    # 按期间排序
    sorted_periods = sorted(periods, key=lambda x: (x[0], x[1] or 0))
    sorted_metrics = sorted(metrics)
    
    # 生成表头
    header = "| 期间 |"
    separator = "|------|"
    for metric in sorted_metrics:
        header += f" {metric} |"
        separator += "--------|"
    output = header + "\n" + separator + "\n"
    
    # 生成数据行
    for period in sorted_periods:
        period_str = f"{year}年" + (f"Q{quarter}" if quarter else "")
        row = f"| {period_str} |"
        
        for metric in sorted_metrics:
            value = data_matrix.get((period formatted = self._, metric))
           format_value(value, unit)
            row += f" {formatted} |"
        
        output += row + "\n"
    
    return output
```

**输出示例**:

```
📊 **ABC公司** 财务数据：

| 期间 | 营业收入 | 净利润 | 毛利率 |
|------|---------|--------|--------|
| 2023年 | 1.50亿 | 1200.00万 | 8.00% |
| 2023年Q1 | 3500.00万 | 280.00万 | 8.00% |
| 2024年 | 1.80亿 | 1500.00万 | 8.33% |
```

### 3.3 列表格式输出

供参考方案文件: `modules/financial_query.py`，行号 1294-1323

```python
def _format_as_list(self, results: List[Dict]) -> str:
    """生成列表格式"""
    output = ""
    for result in results:
        # 格式化数值（自动单位转换）
        formatted_value = self._format_value(value, unit)
        
        period = f"{result['year']}年"
        if result.get('quarter'):
            period += f"Q{result['quarter']}"
        
        output += f"- **{result['metric_name']}** ({period}): {formatted_value}\n"
    
    return output
```

**输出示例**:

```
📊 **ABC公司** 财务数据：

- **营业收入** (2023年): 1.50亿元
- **净利润** (2023年): 1200.00万元
- **毛利率** (2023年): 8.00%
```

### 3.4 数值格式化

供参考方案文件: `modules/financial_query.py`，行号 1225-1253

```python
def _format_value(self, value, unit: str) -> str:
    """格式化数值 - 自动单位转换"""
    if value is None:
        return "暂无数据"
    
    if unit == '元':
        if abs(value) >= 100000000:
            return f"{value/100000000:.2f}亿"
        elif abs(value) >= 10000:
            return f"{value/10000:.2f}万"
        else:
            return f"{value:.2f}"
    else:
        return f"{value:.2f}{unit}"

def _format_change(self, change, unit: str) -> str:
    """格式化增长额（带正负号）"""
    if unit == '元':
        if abs(change) >= 100000000:
            return f"{change/100000000:+.2f}亿"
        elif abs(change) >= 10000:
            return f"{change/10000:+.2f}万"
        else:
            return f"{change:+.2f}"
    else:
        return f"{change:+.2f}{unit}"
```

---

## 四、对比分析功能

### 4.1 对比计算

供参考方案文件: `modules/financial_query.py`，行号 1011-1095

```python
def calculate_comparison(self, results: List[Dict], time_range: Dict) -> Dict:
    """计算对比分析结果"""
    if not time_range.get('is_comparison') or len(results) < 2:
        return {'has_comparison': False}
    
    comparisons = []
    for metric, data in metrics_data.items():
        # 计算第一个和最后一个时间点的差异
        change = last_val - first_val
        change_pct = (change / first_val * 100) if first_val != 0 else None
        
        # 判断趋势
        if change_pct > 1:
            trend = 'up'
        elif change_pct < -1:
            trend = 'down'
        else:
            trend = 'stable'
        
        comparisons.append({
            'metric': metric,
            'periods': [...],
            'first_value': first_val,
            'last_value': last_val,
            'change': change,
            'change_pct': change_pct,
            'trend': trend
        })
    
    return {'has_comparison': True, 'comparisons': comparisons}
```

### 4.2 对比结果格式化

供参考方案文件: `modules/financial_query.py`，行号 1097-1197

```python
def format_comparison(self, comparison_result: Dict, company: Dict) -> str:
    """格式化对比分析结果"""
    # 判断是否需要表格
    use_table = total_periods >= 4 or len(comparisons) > 1
    
    if use_table:
        # 表格格式
        output = """
| 指标 | 起始值 | 最终值 | 变化额 | 增长率 | 趋势 |
|------|--------|--------|--------|--------|------|
| 营业收入 | 2023年: 1.50亿 | 2024年: 1.80亿 | +3000.00万 | +20.0% | 📈 |
"""
    else:
        # 列表格式
        output = """
- **营业收入**: 2023年 → 2024年
  - 变化额: +3000.00万元 📈
  - 增长率: +20.00%
"""
```

---

## 五、图表数据生成

### 5.1 图表类型决策

供参考方案文件: `server/routers/chat.py`，行号 600-674

```python
# 决策逻辑
if len(unique_metrics) == 1:
    # 单指标: 使用 Combo Chart (柱状图 + 增长率)
    chart_data = {"chartType": "combo", ...}
else:
    # 多指标: 发送两个图表
    # 1. 绝对值对比柱状图
    # 2. 增长率对比柱状图
```

### 5.2 Combo Chart (单指标)

供参考方案文件: `server/routers/chat.py`，行号 612-674

```python
# 构建 Combo Chart 数据
chart_data = {
    "chartType": "combo",
    "title": "ABC公司 营业收入趋势分析",
    "labels": ["2023年", "2023年Q1", "2024年"],
    "datasets": [
        {
            "type": "bar",
            "label": "营业收入",
            "data": [150000000, 35000000, 180000000],
            "yAxisID": "y",
            "backgroundColor": "rgba(54, 162, 235, 0.8)"
        },
        {
            "type": "bar",
            "label": "增长率(%)",
            "data": [None, None, 20.0],
            "yAxisID": "y1",
            "backgroundColor": "rgba(255, 159, 64, 0.7)"
        }
    ],
    "options": {
        "scales": {
            "y": {"type": "linear", "position": "left"},
            "y1": {"type": "linear", "position": "right", "grid": {"drawOnChartArea": False}}
        }
    }
}
```

### 5.3 多指标图表

供参考方案文件: `server/routers/chat.py`，行号 676-759

```python
# 图表1: 绝对值对比
chart1_data = {
    "chartType": "bar",
    "title": "ABC公司 指标绝对值对比 (3/5)",
    "labels": ["2023年", "2024年"],
    "datasets": [
        {"label": "营业收入", "data": [150000000, 180000000], ...},
        {"label": "净利润", "data": [12000000, 15000000], ...},
        {"label": "毛利率", "data": [8.0, 8.33], ...}
    ]
}

# 图表2: 增长率对比
chart2_data = {
    "chartType": "bar",
    "title": "ABC公司 指标增长率对比 (%) (3/5)",
    "datasets": [...]
}
```


### 5.4 SSE 事件推送

供参考方案文件: `server/routers/chat.py`，行号 237-239

```python
def send_event(event_type: str, data: dict) -> str:
    """格式化 SSE 事件"""
    return f"event: {event_type}\ndata: {json.dumps(data, ensure_ascii=False)}\n\n"

# 推送图表事件
yield send_event("chart", chart_data)
```


---

## 六、前端渲染

### 6.1 AIChat 组件

供参考方案文件: `frontend/src/components/AIChat.jsx`

```javascript
// 处理 SSE 事件
const eventSource = new EventSource(`/api/chat/stream?question=${encodeURIComponent(question)}`);

eventSource.onmessage = (event) => {
    const data = JSON.parse(event.data);
    
    if (event.type === 'message') {
        // 渲染文本内容
        setMessages(prev => [...prev, { role: 'assistant', content: data.content }]);
    }
    else if (event.type === 'chart') {
        // 保存图表数据
        setCharts(prev => [...prev, data]);
    }
};
```

### 6.2 ChartRenderer 组件

供参考方案文件: `frontend/src/components/ChartRenderer.jsx`

```javascript
import { Bar, Line } from 'react-chartjs-2';

// 注册 Chart.js 组件
ChartJS.register(
    CategoryScale, LinearScale, BarElement, LineElement,
    PointElement, Title, Tooltip, Legend, Filler
);

function ChartRenderer({ chartData, canvasId }) {
    const { chartType, title, labels, datasets, options } = chartData;
    
    // 格式化数据集
    const formattedDatasets = datasets.map((ds, idx) => ({
        ...ds,
        backgroundColor: COLORS[idx % COLORS.length],
        borderColor: BORDER_COLORS[idx % COLORS.length],
        borderWidth: 1,
        borderRadius: 4,
        fill: chartType === 'line' ? 'origin' : undefined
    }));
    
    // 渲染图表
    return (
        <div className="chart-container">
            {chartType === 'bar' && <Bar data={{ labels, datasets: formattedDatasets }} options={options} />}
            {chartType === 'combo' && <Bar data={{ labels, datasets: formattedDatasets }} options={options} />}
        </div>
    );
}
```

### 6.3 双轴对齐核心逻辑

供参考方案文件: `frontend/src/components/ChartRenderer.jsx`，行号 120-196

```javascript
// 双轴 0 刻度对齐核心逻辑
if (finalScales.y && finalScales.y1) {
    // 1. 提取两个数据集的值
    const yData = datasets.find(d => d.yAxisID === 'y')?.data || [];
    const y1Data = datasets.find(d => d.yAxisID === 'y1')?.data || [];
    
    // 2. 计算各轴范围
    const yRange = getRange(yData);
    const y1Range = getRange(y1Data);
    
    // 3. 计算 0 线位置比例
    const r1 = getZeroRatio(yRange);  // -Min / (Max - Min)
    const r2 = getZeroRatio(y1Range);
    
    // 4. 取最大比例作为目标（确保不截断数据）
    const targetRatio = Math.max(r1, r2);
    
    // 5. 根据目标比例反推新的 Min/Max
    finalScales.y.min = newYRange.min;
    finalScales.y.max = newYRange.max;
    finalScales.y1.min = newY1Range.min;
    finalScales.y1.max = newY1Range.max;
}
```

### 6.4 Y 轴数值格式化

供参考方案文件: `frontend/src/components/ChartRenderer.jsx`，行号 79-100

```javascript
// 默认 Scales 配置
const defaultScales = {
    y: {
        beginAtZero: true,
        ticks: {
            callback: function (value) {
                if (Math.abs(value) >= 100000000) {
                    return (value / 100000000).toFixed(1) + '亿';
                } else if (Math.abs(value) >= 10000) {
                    return (value / 10000).toFixed(0) + '万';
                }
                return value;
            }
        }
    }
};
```

---

## 七、完整数据流示例

### 7.1 用户输入

```
ABC公司2023年和2024年的营业收入和净利润对比
```

### 7.2 后端处理流程

供参考方案：略

fintax_ai项目保持9域查询逻辑和方法不变

### 7.3 SSE 推送序列

供参考方案：略

---

## 八、关键配置 【仅供供参考】

### 8.1 指标配置 【仅供供参考】

供参考方案：略

fintax_ai项目保持9域查询逻辑和方法不变


### 8.2 查询设置 【仅供供参考】

供参考方案：略

fintax_ai项目保持9域查询逻辑和方法不变

---

## 九、扩展指南 【仅供供参考】

### 9.1 添加新指标

供参考方案：略

fintax_ai项目保持9域查询逻辑和方法不变

### 9.2 自定义图表类型 【仅供供参考】

供参考方案：修改 `server/routers/chat.py` 中的图表生成逻辑：

```python
# 添加新的图表类型
if some_condition:
    chart_data = {
        "chartType": "line",  # 改为折线图
        ...
    }
```

### 9.3 添加新的格式化样式 【仅供供参考】

供参考方案：修改 `modules/financial_query.py` 中的格式化方法：

```python
def _format_as_cards(self, results, metrics, periods) -> str:
    """生成卡片格式"""
    # 自定义卡片样式输出
    ...
```

---

## 附录 A: 文件清单【供参考项目D:\MyProjects\MCP_coze】

| 文件路径 | 说明 |
|----------|------|
| `modules/financial_query.py` | 财务查询核心模块 |
| `server/routers/chat.py` | API 端点与 SSE 推送 |
| `frontend/src/components/AIChat.jsx` | 聊天界面组件 |
| `frontend/src/components/ChartRenderer.jsx` | 图表渲染组件 |
| `config/metrics_config.json` | 指标配置 |

---

## 附录 B: SSE 事件类型【供参考项目】

| 事件类型 | 说明 | 数据格式 |
|----------|------|----------|
| `start` | 开始处理 | `{"status": "processing"}` |
| `message` | 文本内容 | `{"content": "..."}` |
| `chart` | 图表数据 | `{"chartType": "...", "labels": [...], "datasets": [...]}` |
| `summary` | 分析总结 | `{"content": "..."}` |
| `error` | 错误信息 | `{"message": "..."}` |
| `done` | 完成 | `{"status": "completed"}` |

---

## 附录 C: 图表数据结构

```typescript
interface ChartData {
    chartType: 'bar' | 'line' | 'combo';
    title: string;
    labels: string[];
    datasets: Dataset[];
    options?: {
        scales?: {
            y?: ScaleOptions;
            y1?: ScaleOptions;
        };
    };
}

interface Dataset {
    type: 'bar' | 'line';
    label: string;
    data: number[];
    yAxisID?: 'y' | 'y1';
    backgroundColor?: string;
    borderColor?: string;
}
```

